python3 -m venv .venv
. .venv/bin/activate
python -m ensurepip --upgrade
python -m pip install -r requirements/runtime.txt
scripts/codegen_schemas.sh
python3 scripts/generate_ed25519_keypair.py # You may generate your own private and public keypair and copy it into the .env or simply use the ones provided. In latter case, skip this step.
scripts/build_frontend.sh
scripts/seed_memory.sh


Change Log and Next Planned. 
