[project]
name = "core-storage"
version = "0.1.0"
description = "ArangoDB adapters for BatVault"
requires-python = ">=3.11"
dependencies = ["python-arango>=7.9.1", "pydantic>=2.7.0"]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
